# Testing Standard

## 测试策略

- 核心业务逻辑必须有单元测试
- 跨模块流程必须有集成测试
- 用户关键路径需要冒烟测试

## 测试要求

- 测试名称表达业务行为
- 覆盖正常路径、异常路径和边界条件
- 不为了覆盖率编写无意义测试

## 运行记录

每次 `/sdc:check` 后，将测试结果记录到 `.sdc/reports/` 或当前 change 的 `notes.md`。
